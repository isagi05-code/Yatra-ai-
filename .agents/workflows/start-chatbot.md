---
description: start chatbot backend server
---
# Start Backend
This workflow will start the `app.py` server on port 5000 so the AI assistant works in the chat.

1. Ensure you are in the project folder.
// turbo
2. Run `py app.py` to start the server.
